<% flash.each do |name, msg| %>
  <% unless flash_blacklist(name) %>
    <%= content_tag(:div, class: flash_class(name), role: 'alert') do %>
      <%= msg %>
      <button type="button" class="close" data-dismiss="alert">×</button>
    <% end %>
  <% end %>
<% end %>
